# 项目协作规范

以下规则适用于本仓库中的所有修改：

1. 每次修改代码、配置或文档时，必须同步更新 `README.md`，确保功能、配置、运行方式和当前实现保持一致。
2. 修改完成后必须运行与改动相匹配的检查或测试，并查看 `git diff` 与 `git status`。
3. 每轮修改完成后必须创建 Git 提交；除非用户明确要求不提交，不得把已完成的改动留在工作区。
4. 提交应保持主题单一，提交信息清楚说明改动目的，不混入无关文件。
5. 向用户交付时必须展示：
   - 提交哈希与提交信息；
   - 修改文件列表和主要修改细节；
   - 已执行的测试及结果；
   - 最终工作区状态。

